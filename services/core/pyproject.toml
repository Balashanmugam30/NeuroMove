[project]
name = "neuromove-core"
version = "0.1.0"
description = "NeuroMove Local Core Control Station Service"
requires-python = ">=3.11"
dependencies = [
    "neuromove",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
